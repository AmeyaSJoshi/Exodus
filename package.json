{
  "name": "egress-workspace",
  "private": true,
  "description": "EGRESS — repo-local tooling. The iOS app lives in EgressMapper/.",
  "scripts": {
    "db:start": "supabase start",
    "db:reset": "supabase db reset",
    "db:stop": "supabase stop",
    "db:verify": "psql \"$DATABASE_URL\" -v ON_ERROR_STOP=1 -f supabase/tests/security_checks.sql"
  },
  "devDependencies": {
    "supabase": "^2.111.0"
  }
}
