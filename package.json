{
  "name": "egress-workspace",
  "private": true,
  "description": "EGRESS \u2014 repo-local tooling. The iOS app lives in EgressMapper/.",
  "scripts": {
    "db:start": "supabase start",
    "db:reset": "supabase db reset",
    "db:stop": "supabase stop",
    "db:verify": "docker exec -i supabase_db_egress psql -U postgres -v ON_ERROR_STOP=1 < supabase/verification/security_checks.sql"
  },
  "devDependencies": {
    "supabase": "^2.111.0"
  }
}
